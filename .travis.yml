language: r
cache: packages
r:
    - release

r_packages:
    - Rcpp
    - RcppArmadillo

pandoc: false

sudo: false

branches:
    only:
        - master

install: true

script:
    - lscpu
    - Rscript Test/irisTest.R
