language: r
dist: trusty
cache: packages

r:
  - release

sudo: false

addons:
  apt:
    packages:
      - libgsl0-dev

r_packages:
  - devtools
  - testthat

script:
  #- Rscript .#build.R
  - R -e "devtools::install(".")"
  - cd tests
  - printf "starting testthat run\n"
  - Rscript testthat.R